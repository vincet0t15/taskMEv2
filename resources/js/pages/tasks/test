{auth.user?.id ===
                                                comment.user.id && (
                                                <div className="mt-2 ml-9 flex items-center gap-2 text-sm text-gray-600 italic">
                                                    <span className="cursor-pointer hover:text-blue-600 hover:underline">
                                                        Edit
                                                    </span>
                                                    <span className="cursor-pointer hover:text-red-600 hover:underline">
                                                        Delete
                                                    </span>
                                                </div>
                                            )}
